const BaseUrl = 'http://localhost:4000';
// const BaseUrl = 'http://localhost:5000';

export const blogPath = `${BaseUrl}/blog`;
export const userPath = `${BaseUrl}/user`;
export const organisationPath = `${BaseUrl}/organisation`;

